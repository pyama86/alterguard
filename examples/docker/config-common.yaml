pt_osc:
  charset: utf8mb4
  recursion_method: "dsn=D=testdb,t=dsns"
  no_swap_tables: false
  chunk_size: 1000
  max_lag: 1.5
  statistics: true
  dry_run: false

alert:
  metadata_lock_threshold_seconds: 30
